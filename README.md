# WaqfChain — Waqf on Blockchain

The first-ever website dedicated to the vision of bringing Islamic Waqf endowments onto the blockchain: immutable deeds, transparent yields, perpetual charity (sadaqah jariyah).

Built with Next.js (App Router). Deployed on Vercel.

```bash
npm install
npm run dev
```
